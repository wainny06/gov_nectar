# Notion 연결 설정

관리 공간: https://www.notion.so/3db690c9fd5181b5bafee3fd6459006f

초기 등록: 저장소의 지원사업 29개, 준비 작업 116개. 사용자의 브라우저에만 저장된 수정/추가 공고는 첫 동기화 시 반영됩니다.

1. Notion 개발자 포털에서 해당 워크스페이스의 내부 연결을 만들고 콘텐츠 읽기·삽입·수정 권한을 켭니다. https://developers.notion.com/
2. Notion의 위 관리 페이지에서 연결 추가 메뉴로 이 내부 연결에 접근을 부여합니다. 하위 두 데이터베이스 모두 접근 가능해야 합니다.
3. Vercel 프로젝트 Settings → Environment Variables에 `NOTION_TOKEN`을 내부 연결의 토큰으로 등록합니다. 토큰을 채팅이나 GitHub 파일에 넣지 마세요.
4. `NOTION_SYNC_SECRET`에 무작위 24자 이상의 별도 문자열을 등록합니다. 예: 로컬에서 `openssl rand -hex 24`로 생성. Production 환경에 적용하고 Redeploy 합니다. Preview는 필요한 경우에만 별도 설정합니다.
5. 대시보드 하단 Notion 연동의 동기화 키에 `NOTION_SYNC_SECRET` 값을 입력하고 **연결 진단**을 누릅니다. 두 데이터베이스 접근·필드 확인 완료가 나오면 **Notion 업데이트**를 누릅니다. 진단은 데이터를 변경하지 않으며 삽입·수정 권한은 첫 동기화로 확인합니다. **Notion API 토큰을 이 입력칸에 넣지 마세요.** 키는 페이지 메모리에만 보관되고 새로고침하면 사라집니다.
6. 한 항목의 메모를 변경하여 재동기화한 후 Notion의 대시보드메모에 반영되는지 확인합니다. Notion에서 수정한 담당자·상태·목표일·관리메모가 유지되는지도 확인합니다.

## 사용 방식

현재 공고 / 2027 준비 / 인증·공모 세 공간을 함께 보냅니다. 첫 전체 전송은 수 분 걸릴 수 있습니다. 중단되어도 같은 ID를 조회해 재시도하므로 순차 재실행 시 중복 생성하지 않습니다. 같은 브라우저 탭에서는 완료된 동일 내용은 건너뜁니다. 동기화는 한 탭에서 실행하세요. 여러 사용자/탭의 동시 최초 생성은 Notion의 고유키 제약 부재로 중복될 수 있습니다. 중복 ID를 발견하면 덮어쓰지 않고 오류로 알립니다.

‘이 탭에서 저장 후 자동 업데이트’를 켜면 저장 또는 공고 새로고침 후 5초 뒤 변경 항목을 전송합니다. 탭을 닫으면 실행되지 않으며 브라우저 전체 새로고침 후에는 키를 다시 입력합니다. 실패하면 표시된 오류를 해결하고 버튼을 눌러 이어서 실행합니다. 예약 크롤링은 없습니다.

대시보드가 관리하는 값: 사업명·지원내용·조건·원문·확정마감·대시보드메모, 작업명·권장일·대시보드담당자·대시보드완료.
Notion이 관리하는 값: 담당자·상태·목표일·관리메모·페이지 본문. 최초 작업 생성 시에만 권장일/완료 상태로 목표일/상태를 초기화합니다. 그 이후 대시보드 변경은 별도의 대시보드 속성에 반영되며 Notion의 계획을 덮어쓰지 않습니다. 목표일과 권장일이 달라지면 Notion에서 비교 후 조정하세요.

삭제/보관 상태는 전파하지 않습니다. Notion 작업 이력을 보존하고 필요하면 직접 보류 처리합니다. Notion에서 동기화된 행을 삭제하면 다음 전체 전송에서 재생성될 수 있습니다. 동기화ID와 데이터베이스 속성 이름을 변경하지 마세요.

2027 준비일은 내부 목표이며 확정 공고 마감일이 아닙니다. 데이터 소스 ID는 lib/notion-records.js에 고정되어 있습니다. 다른 Notion 공간으로 옮길 경우 스키마와 ID를 함께 변경해야 합니다.

현재 ChatGPT의 Notion 연결 권한은 Vercel 런타임 토큰으로 전달되지 않습니다. 위 1회 설정 전에는 초기 등록된 Notion 관리 기능만 사용할 수 있습니다.

## 연결 문제 확인 순서

`GET /api/notion-sync`의 `configured: true`는 환경변수 존재·동기화 키 길이만 확인합니다. 실제 Notion 연결 성공을 뜻하지 않습니다. 연결 진단은 동일 API에 인증된 POST `{ "action": "diagnose" }`를 보내 두 데이터 소스의 속성 이름·유형, 작업의 지원사업 관계 대상, 최초 상태 선택값을 확인합니다.

| 표시 또는 증상 | 조치 |
| --- | --- |
| 설정 대기 / 503 | 실제 운영 프로젝트의 Production에 NOTION_TOKEN 및 24자 이상 NOTION_SYNC_SECRET이 있는지 확인하고 재배포 |
| 동기화 키 확인 / 401 | 대시보드에는 NOTION_SYNC_SECRET 입력. API 토큰과 구분 |
| Notion 토큰 무효 | NOTION_TOKEN 값·폐기 여부 확인 후 재배포 |
| Notion 권한 부족 | 내부 연결의 콘텐츠 읽기·삽입·수정 권한 확인 |
| Notion 데이터 접근 불가 | 관리 페이지와 두 하위 DB를 내부 연결에 공유했는지, 코드의 데이터 소스 ID가 맞는지 확인 |
| 데이터베이스 구조 확인 필요 | 오류에 표시된 필드 이름·유형·관계 대상 복구. 상태는 select 유형이며 할 일/완료 선택값 필요 |
| 요청 한도 | Notion Retry-After에 따라 최대 3회 자동 재시도. 60초 초과 대기 지시는 안내 후 중단 |
| 응답 미확인 / 시간 초과 / 서버 오류 | 생성이 반영됐을 수 있어 자동 재시도하지 않음. 잠시 기다린 뒤 버튼으로 재실행하면 동기화ID로 기존 행을 조회 |
| 로그인 화면 / HTML 응답 / 302 | Vercel 배포 보호 로그인과 현재 운영 도메인 확인. Notion 토큰 문제와 별개 |

연결 진단은 쓰기 권한·전체 행의 중복 여부를 보증하지 않습니다. 한 탭에서 첫 전송을 완료한 뒤 메모 한 항목을 변경해 재전송하고, 기존 Notion의 담당자·상태·목표일·관리메모가 유지되는지 확인해야 연결 완료입니다.

## 2026-09-14 점검 기록

- Notion 두 데이터 소스의 ID와 현재 속성 구조는 코드와 일치함을 확인했습니다.
- 연결 진단, 구분된 오류 안내, 제한된 요청 한도 재시도를 추가했습니다. 예약 실행은 추가하지 않았습니다.
- 점검 당시 연결된 Vercel 팀의 프로젝트 목록은 비어 있었고, `beauty-support-desk` 조회는 404였습니다. 문서의 배포 URL은 Vercel 로그인으로 302 이동했습니다. 따라서 운영 환경변수 등록 여부와 실제 Notion 쓰기 성공은 미확인입니다.
- Vercel에서 Git 연결이 `wainny06/gov_nectar`인 실제 프로젝트를 열어 Production 환경변수와 최신 배포 커밋을 확인하세요. 로그인 보호를 해제할 필요는 없습니다.

참고: [Notion 데이터 소스 조회](https://developers.notion.com/reference/retrieve-a-data-source), [Notion 요청 한도와 Retry-After](https://developers.notion.com/reference/request-limits).
